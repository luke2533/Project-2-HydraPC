function savePc(){
  var name = prompt("Please enter your full name")
  var msg;
  if (name == null || name == ""){
    msg = "User cancelled save.";
  } else {
    var email = prompt("Please enter your Email", "")
    if (email == null || email == ""){
    msg = "User cancelled save.";
  } else {
    //this is where you add the emailJS
  }
  }
};
